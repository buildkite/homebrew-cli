# Generated by https://github.com/buildkite/bk/blob/master/.github/tap-release.yml

class Bk < Formula
  homepage "https://github.com/buildkite/cli"
  desc "A command-line tool for working with Buildkite.com"
  url "$STABLE_URL"
  version "$STABLE_VERSION"
  sha256 "$STABLE_SHA256"

  def install
    prefix.mv "bk-darwin-amd64-$VERSION_NUMBER" "bk"
    bin.install "bk"
  end

  test do
    assert_match "bk #{version}", shell_output("#{bin}/bk --version 2>&1")
  end
end
